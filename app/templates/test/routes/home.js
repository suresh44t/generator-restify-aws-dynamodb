/**
 * Created by maksym on 3/16/16.
 */
const chai   = require('chai');
const expect = chai.expect;
describe("#lateGreets", () => {
    it("should pass an error if no target is passed", () => {
        expect(false).to.be.false;
    });
});